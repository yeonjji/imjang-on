/**
 * 1회성: "전세가율이 높으면 좋은 아파트일까, 위험한 아파트일까" 글을 DRAFT로 넣는다.
 * 검수·게시는 /admin/posts.
 *
 * 같은 dedupeKey가 DRAFT로 있으면 본문을 갱신한다(검수 지적 반영용). PUBLISHED면 건드리지 않는다.
 *
 * 실행:
 *   pnpm exec dotenv -e .env.test -- tsx scripts/board/insert-jeonse-ratio-meaning.ts --dry-run
 *   온박스: etl 컨테이너에 이 파일만 바인드마운트해 실행
 */
import { prisma } from '@/lib/db';
import { logger } from '@/lib/logger';
import type { GenerateResult } from '@/lib/board/generate';
import { createDraft } from '@/lib/board/create-draft';
import { buildBoardSlug } from '@/lib/board/slug';
import { runGuardrails, MAX_BODY_CHARS_MANUAL } from '@/lib/board/guardrails';

const DEDUPE_KEY = 'manual:jeonse-ratio-meaning';

/**
 * VERIFIED 2026-09-14 — 운영 DB 읽기전용 자체 집계. 본문 수치는 전부 여기서 나왔다.
 *
 * [산식 — 사이트 상세 페이지와 동일하게 맞췄다]
 *   lib/transaction.ts:212-258(getAreaSummary)의 정의를 그대로 복제했다. 정의가 갈리면 글의 수치와
 *   화면의 전세가율이 어긋난다.
 *     평형   = ROUND(전용면적 / 3.3057851239669422)
 *     전세가율 = (동일 평형 전세 12개월 평균 보증금) / (동일 평형 매매 12개월 평균가) × 100
 *     게이트  = 같은 평형에서 매매 2건 이상 AND 전세 2건 이상 (평형 혼합·단건 노이즈 배제)
 *
 * [집계 결과 — 최근 12개월, 아파트 29,881개 (단지, 평형) 조합 / 16,399개 단지]
 *   분포     중앙값 69.0% · 25% 지점 57.2% · 75% 지점 78.8%
 *            80% 이상 22.3% · 90% 이상 6.3% · 100% 이상 1.1%
 *   시도별   경북 82.0 · 충북 81.7 · 전북 81.5 · 충남 80.3 · 강원 79.7 · 경남 79.6 ·
 *            전남광주 78.8 · 울산 76.4 · 대전 73.7 · 인천 70.4 · 대구 69.7 · 부산 69.1 ·
 *            경기 68.5 · 서울 52.4 · 세종 46.7  (표본 200조합 이상인 15곳)
 *            ※ 시도 코드는 Region 테이블 이름으로 확인했다. 2026-07 개편으로 전남광주통합특별시(12),
 *              강원특별자치도(51), 전북특별자치도(52) 신코드가 있어 코드 추측이 불가능하다.
 *
 * [★ 상승 원인 분해 — 직전 12개월과 비교 가능한 24,386조합]
 *   전세가율 상승 13,944(57.2%) / 하락 10,441.
 *   상승분 13,944를 매매·전세 변동(데드밴드 ±2%)으로 가르면
 *     전세 상승이 주원인          8,458 (60.7%)
 *     둘 다(매매 하락 + 전세 상승) 2,723 (19.5%)
 *     매매 하락이 주원인          2,144 (15.4%)
 *     둘 다 보합권                  619 ( 4.4%)
 *   검산: 8,458+2,723+2,144+619 = 13,944 ✅ (5)번 집계의 상승 건수와 일치.
 *   매매 하락이 끼어 있는 경우 = 2,723+2,144 = 4,867 → 34.9%.
 *   상승 조합의 변동 중앙값: 매매 0.0%, 전세 +7.3%.
 *
 * [전세가율 90% 이상] 1,874조합. 매매가 - 보증금 갭 중앙값 637만원.
 *
 * [유형별 커버리지 — 분모를 같은 성격으로 맞췄다]
 *   분모 = 최근 12개월에 매매와 전세 거래가 **둘 다** 있는 매물(평형 무관).
 *   분자 = 그중 같은 평형에서 각각 2건 이상이라 실제로 전세가율이 계산되는 매물.
 *     아파트   22,727 → 16,399 (72.2%)
 *     오피스텔  4,277 →  2,161 (50.5%)
 *     연립      2,298 →    458 (19.9%)
 *     다세대   14,715 →  1,514 (10.3%)
 *   유형별 전세가율 중앙값: 오피스텔 91.3(100% 이상 22.8%) · 아파트 69.0 · 연립 66.9 · 다세대 62.8.
 *
 * [의도적으로 안 쓴 것]
 *   갭투자·전세사기 수법 설명(기존 글 4번과 중복), 보증보험 가입 요건(기존 글 38번), 근저당 순위
 *   (기존 글 85번) — 맥락 링크로만 연결했다. 특정 단지·지역 추천이나 시점 판단은 쓰지 않는다.
 */
const SOURCE = {
  sourceName: '국토교통부 실거래가 공개시스템',
  sourceUrl: 'https://rt.molit.go.kr',
  // 원문 발행일이 아니라 집계 기준일이다(sourceDateIsPublication=false).
  sourceDate: new Date('2026-09-14T00:00:00Z'),
  detectedFrom: 'manual:jeonse-ratio-meaning',
  sourceExcerpt: `[자체 집계 — 국토교통부 실거래가 공개시스템 적재분, 기준일 2026-09-14, 최근 12개월]
[산식] 사이트 상세 페이지(getAreaSummary)와 동일. 평형 = ROUND(전용면적/3.3057851239669422). 전세가율 = 동일 평형 전세 12개월 평균 보증금 / 동일 평형 매매 12개월 평균가 × 100. 게이트: 같은 평형에서 매매 2건 이상 AND 전세 2건 이상(평형 혼합·단건 노이즈 배제).
[표본] 아파트 29,881개 (단지, 평형) 조합 / 16,399개 단지.
[분포] 중앙값 69.0%. 25% 지점 57.2%, 75% 지점 78.8%. 80% 이상 22.3%, 90% 이상 6.3%, 100% 이상 1.1%.
[시도별 중앙값] 경상북도 82.0 / 충청북도 81.7 / 전북특별자치도 81.5 / 충청남도 80.3 / 강원특별자치도 79.7 / 경상남도 79.6 / 전남광주통합특별시 78.8 / 울산광역시 76.4 / 대전광역시 73.7 / 인천광역시 70.4 / 대구광역시 69.7 / 부산광역시 69.1 / 경기도 68.5 / 서울특별시 52.4 / 세종특별자치시 46.7. 표본 200조합 이상인 15곳. 시도 코드는 Region 테이블 이름으로 확인(2026-07 행정구역 개편 신코드 존재).
[상승 원인 분해] 직전 12개월과 비교 가능한 24,386조합 중 전세가율 상승 13,944(57.2%), 하락 10,441. 상승 13,944건을 매매·전세 변동(데드밴드 ±2%)으로 분해: 전세 상승이 주원인 8,458(60.7%), 둘 다(매매 하락+전세 상승) 2,723(19.5%), 매매 하락이 주원인 2,144(15.4%), 둘 다 보합권 619(4.4%). 합 13,944로 검산 일치. 매매 하락이 끼어 있는 경우 4,867(34.9%). 상승 조합의 변동 중앙값은 매매 0.0%, 전세 +7.3%.
[전세가율 90% 이상] 1,874조합. 매매 12개월 평균가에서 전세 12개월 평균 보증금을 뺀 갭의 중앙값 637만원.
[유형별 계산 가능 비율] 분모는 최근 12개월에 매매·전세 거래가 둘 다 있는 매물, 분자는 같은 평형에서 각각 2건 이상인 매물. 아파트 22,727→16,399(72.2%), 오피스텔 4,277→2,161(50.5%), 연립 2,298→458(19.9%), 다세대 14,715→1,514(10.3%).
[유형별 전세가율 중앙값] 오피스텔 91.3(100% 이상 22.8%), 아파트 69.0, 연립 66.9, 다세대 62.8.`,
} as const;

const GEN: GenerateResult = {
  type: 'TREND',
  category: 'REALESTATE',
  title: '전세가율 80%는 좋은 신호일까',
  summary:
    '전세가율이 높으면 실거주 수요가 탄탄하다고도 하고, 위험하다고도 합니다. 최근 12개월 아파트 실거래에서 전세가율이 오른 단지를 분해해 보니 전세가 올라서인 경우가 60.7%, 매매가 떨어진 쪽이 섞인 경우가 34.9%였습니다. 같은 80%라도 이유가 다릅니다.',
  body: `전세가율이 높으면 실거주 수요가 탄탄한 단지라고도 하고, 반대로 너무 높으면 위험하다고도 합니다. 둘 다 맞을 수 있습니다. 숫자 자체보다 그 숫자가 어떻게 만들어졌는지가 갈리기 때문입니다.

전세가율은 매매가격 대비 전세가격의 비율입니다. 매매 5억 원에 전세 4억 원이면 80%입니다. 이 글의 수치는 임장온이 국토교통부 실거래가를 직접 집계한 값이며, 상세 페이지와 같은 기준을 씁니다. 같은 단지라도 평형을 맞춰 계산하고, 그 평형에서 최근 12개월 매매와 전세가 각각 2건 이상일 때만 냅니다. 평형을 섞거나 한 건으로 계산한 비율은 쓰지 않습니다.

## 실제 아파트 전세가율은 69%입니다

이 기준을 통과한 아파트는 16,399개 단지, 29,881개 (단지·평형) 조합입니다.

| 구간 | 전세가율 |
|---|---|
| 낮은 쪽 25% 지점 | 57.2% |
| 중앙값 | 69.0% |
| 높은 쪽 25% 지점 | 78.8% |

80% 이상은 22.3%, 90% 이상은 6.3%, 보증금이 매매가를 넘긴 100% 이상은 1.1%입니다. 흔히 기준선처럼 말하는 80%는 실제로는 상위 4분의 1에 해당합니다.

지역 차이가 상당히 큽니다. 중앙값이 가장 높은 경상북도가 82.0%, 가장 낮은 세종특별자치시가 46.7%입니다. 서울특별시는 52.4%로, 80%를 넘는 조합이 2.7%뿐입니다. 매매가가 높은 지역일수록 전세가율이 낮게 나옵니다. "전세가율 몇 % 이상은 위험하다"를 전국에 똑같이 적용하기 어려운 이유입니다.

## 전세가율이 올랐다고 전세시장이 좋아진 것은 아닙니다

전세가율은 분자(전세)가 오를 때도 올라가지만, 분모(매매)가 떨어질 때도 올라갑니다. 결과는 같은 80%인데 뜻은 정반대입니다.

직전 12개월과 비교할 수 있는 24,386개 조합 중 전세가율이 오른 것은 13,944개(57.2%)였습니다. 이 13,944개가 왜 올랐는지 매매·전세 변동으로 갈라 봤습니다.

| 무엇이 전세가율을 올렸나 | 조합 | 비중 |
|---|---|---|
| 전세가 올라서 | 8,458 | 60.7% |
| 매매가 떨어지고 전세가 올라서 | 2,723 | 19.5% |
| 매매가 떨어져서 | 2,144 | 15.4% |
| 둘 다 보합권 | 619 | 4.4% |

전세 상승이 주된 경우가 가장 많지만, **매매가 하락이 끼어 있는 경우가 4,867개로 34.9%**입니다. 전세가율이 오른 아파트 셋 중 하나는 매매가가 떨어진 쪽이 섞여 있다는 뜻입니다. 상승한 조합의 변동 중앙값은 매매 0.0%, 전세 +7.3%였습니다.

그래서 전세가율이 올랐다는 사실만으로는 방향을 알 수 없습니다. 매매가와 전세가를 각각 따로 봐야 합니다.

"전세가율이 높으면 실거주 수요가 탄탄하다"는 말은 이 60.7%를 두고 하는 이야기에 가깝습니다. 다만 이 집계로 말할 수 있는 것은 전세가가 올랐다는 사실까지이고, 그것이 수요가 늘어서인지 전세 매물이 줄어서인지는 실거래가만으로 가려지지 않습니다. 반대로 전세가율이 내린 조합도 10,441개로 적지 않습니다. 전세가율은 한쪽으로만 움직이는 지표가 아닙니다.

## 높은 전세가율에서 실제로 남는 돈

전세가율이 90%를 넘는 조합은 1,874개였습니다. 이 단지들의 매매 평균가에서 전세 평균 보증금을 뺀 차이는 중앙값 637만 원입니다.

집값이 그만큼만 내려가도 보증금과 매매가가 뒤집힙니다. 집주인이 새 세입자의 보증금으로 기존 보증금을 돌려주는 구조였다면, 전세가가 내리거나 세입자를 구하지 못할 때 반환이 어려워집니다. 전세를 구한다면 비율보다 이 차액이 얼마인지를 금액으로 확인하는 편이 낫습니다. 근저당이 잡혀 있다면 [채권최고액까지 더해서](/board/85) 봐야 합니다.

## 빌라의 전세가율은 아파트처럼 믿기 어렵습니다

전세가율을 계산하려면 같은 평형에서 매매와 전세 실거래가 모두 있어야 합니다. 최근 12개월에 매매와 전세가 둘 다 거래된 매물만 놓고 봐도, 평형까지 맞춰 계산이 되는 비율은 유형별로 크게 갈립니다.

| 유형 | 계산 가능 | 전세가율 중앙값 |
|---|---|---|
| 아파트 | 72.2% | 69.0% |
| 오피스텔 | 50.5% | 91.3% |
| 연립 | 19.9% | 66.9% |
| 다세대(빌라) | 10.3% | 62.8% |

다세대주택은 매매·전세가 둘 다 거래된 14,715곳 중 1,514곳(10.3%)에서만 평형을 맞춘 전세가율이 나옵니다. 집마다 면적과 구조가 달라 비교할 매매 사례가 적기 때문입니다. 빌라에 붙은 전세가율은 오래된 매매 사례나 다른 평형을 섞어 만든 값일 수 있습니다.

오피스텔은 성격이 다릅니다. 중앙값이 91.3%이고, 보증금이 매매가를 넘는 100% 이상이 22.8%입니다. 아파트 기준으로 "90%면 위험하다"고 익힌 감각을 그대로 적용하면 오피스텔은 대부분이 경고 구간에 들어갑니다.

## 볼 때 확인할 것

- 전세가율이 오른 이유가 전세 상승인지 매매 하락인지
- 매매가와 보증금의 차이를 비율이 아니라 금액으로
- 최근 매매 거래가 언제였는지, 같은 평형인지
- 아파트 기준과 오피스텔·빌라 기준을 섞지 않기

전세가율은 결론이 아니라 매매시장과 전세시장의 관계를 보는 지표 하나입니다. 같은 80%라도 전세가 올라 만들어진 80%와 매매가 떨어져 만들어진 80%는 다릅니다.`,
};

async function main() {
  const dryRun = process.argv.includes('--dry-run');
  const guard = runGuardrails({
    body: GEN.body,
    sourceName: SOURCE.sourceName,
    sourceUrl: SOURCE.sourceUrl,
    maxLength: MAX_BODY_CHARS_MANUAL,
  });
  const charCount = GEN.body.replace(/\s/g, '').length;

  console.log(
    `\n[${GEN.type}/${GEN.category}] ${GEN.title}\n${GEN.summary}\n${'-'.repeat(60)}\n${GEN.body}\n${'-'.repeat(60)}\n` +
      `가드레일: ${guard.ok ? 'PASS ✅' : 'FAIL ❌ → ' + guard.violations.join(', ')} (공백제외 ${charCount}자, 발췌 ${SOURCE.sourceExcerpt.length}자)\n`,
  );

  if (dryRun) return;
  if (!guard.ok) {
    logger.error({ violations: guard.violations }, '가드레일 실패 — 미생성');
    process.exitCode = 1;
    return;
  }

  const existing = await prisma.post.findUnique({
    where: { dedupeKey: DEDUPE_KEY },
    select: { id: true, status: true },
  });

  if (existing) {
    if (existing.status !== 'DRAFT') {
      console.log(`이미 ${existing.status}(id=${existing.id}) — 건드리지 않는다. 어드민에서 처리하라.`);
      return;
    }
    // 제목이 바뀌면 slug도 따라가야 기록이 어긋나지 않는다. slug는 정규 경로(/board/<id>)에
    // 쓰이지 않는 레거시 조회 키라 DRAFT 단계에서 바꿔도 걸려 있는 링크가 없다.
    let slug = buildBoardSlug(GEN.title, '2026-09-14');
    for (let i = 2; ; i++) {
      const clash = await prisma.post.findUnique({ where: { slug }, select: { id: true } });
      if (!clash || clash.id === existing.id) break;
      slug = buildBoardSlug(GEN.title, '2026-09-14', i);
    }
    await prisma.post.update({
      where: { id: existing.id },
      data: {
        slug,
        title: GEN.title,
        summary: GEN.summary,
        body: GEN.body,
        sourceName: SOURCE.sourceName,
        sourceUrl: SOURCE.sourceUrl,
        sourceDate: SOURCE.sourceDate,
        sourceExcerpt: SOURCE.sourceExcerpt.slice(0, 4000),
      },
    });
    logger.info({ id: String(existing.id) }, 'DRAFT 갱신 완료 — /admin/posts에서 검수');
    return;
  }

  const res = await createDraft({
    gen: GEN,
    sourceName: SOURCE.sourceName,
    sourceUrl: SOURCE.sourceUrl,
    sourceDate: SOURCE.sourceDate,
    sourceDateIsPublication: false, // 원문 발행일이 아니라 자체 집계 기준일
    sourceExcerpt: SOURCE.sourceExcerpt.slice(0, 4000),
    dedupeKey: DEDUPE_KEY,
    dateISO: '2026-09-14',
    detectedFrom: SOURCE.detectedFrom,
    maxLength: MAX_BODY_CHARS_MANUAL,
  });

  if (res.status === 'created') {
    logger.info({ slug: res.slug, id: String(res.id) }, 'DRAFT 생성 완료 — /admin/posts에서 검수');
  } else if (res.status === 'duplicate') {
    logger.info('dedupeKey 중복 — 건너뜀');
  } else {
    logger.error({ violations: res.violations }, 'createDraft rejected');
    process.exitCode = 1;
  }
}

main()
  .catch((err) => {
    logger.error({ err }, 'insert-jeonse-ratio-meaning fatal');
    process.exit(1);
  })
  .finally(() => {
    void prisma.$disconnect();
  });
